Creació d'una pàgina (View) amb el nom IndexCategory, dins del namespace UdemyCourse.Views i la carpeta Views
```bash
    dotnet new page --name IndexCategory --namespace UdemyCourse.Views --output Views
```